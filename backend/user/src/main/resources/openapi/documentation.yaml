openapi: "3.1.0"
info:
  title: "dena_2024_team2 API"
  description: "dena_2024_team2 API"
  version: "1.0.0"
servers:
- url: "https://dena_2024_team2"
paths:
  /:
    get:
      description: ""
      responses:
        "200":
          description: "OK"
          content:
            text/plain:
              schema:
                type: "string"
              examples:
                Example#1:
                  value: "Hello World!"
  /user/new:
    post:
      description: ""
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserLogin"
        required: true
components:
  schemas:
    UserLogin:
      type: "object"
      properties:
        name:
          type: "string"
        password:
          type: "string"
      required:
      - "name"
      - "password"